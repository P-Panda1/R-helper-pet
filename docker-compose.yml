version: '3.8'

services:
  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - qdrant_storage:/qdrant/storage
    environment:
      - QDRANT__SERVICE__GRPC_PORT=6334

  # Uncomment to run the app in Docker (requires model files)
  # history-helper:
  #   build: .
  #   depends_on:
  #     - qdrant
  #   volumes:
  #     - ./models:/app/models
  #     - ./cache:/app/cache
  #     - ./logs:/app/logs
  #   environment:
  #     - QDRANT_HOST=qdrant
  #     - QDRANT_PORT=6333

volumes:
  qdrant_storage:

